# Build-time dependency. Used on Windows (build.ps1 → the .exe),
# Linux and macOS (build.sh → the binary).
#
# Windows extra: Inno Setup 6 (free): https://jrsoftware.org/isdl.php
#   ISCC.exe expected at "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
#
# Linux runtime requirement (not bundled by PyInstaller):
#   Ubuntu:  sudo apt install libportaudio2

pyinstaller>=6.0.0