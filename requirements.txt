# Runtime dependencies for AudioEnvelope (main.py)
# Install with:  pip install -r requirements.txt
#
# Tested with Python 3.12.  Python 3.11+ should also work.

dearpygui>=2.3.1
numpy>=1.26.4
sounddevice>=0.5.1
psutil>=5.9.0

# scipy is NOT required on Windows — the app uses a pure-Python biquad fallback.
# On Linux/macOS scipy provides a C-level sosfilt that avoids GIL-induced stutters;
# install it manually there if needed:  pip install "scipy>=1.10.0"
# scipy>=1.10.0

